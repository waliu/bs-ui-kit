'use strict';

const bsWebComponentVue = require('..');
const assert = require('assert').strict;

assert.strictEqual(bsWebComponentVue(), 'Hello from bsWebComponentVue');
console.info('bsWebComponentVue tests passed');
